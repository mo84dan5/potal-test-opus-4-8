# FN-015 イベント

## 概要
NPC等をタップすると始まる**スクリプト化イベント**。例: 話しかけると主人公が実際に歩いて案内される、
岩が動いて道が開く。イベント中は**見回し(視点回転)以外の操作は不能**。

## イベントの構成
イベントは `EventStep` の列(`GameEvent.steps`)。`Interactable.event` に持たせ、タップで開始する。

| ステップ | 内容 |
| --- | --- |
| `say` | 一定時間メッセージを表示(`eventMessage`)。タップ送りではない |
| `walkTo` | 主人公を (x,z) まで自動で歩かせる(目的地方向へ `desiredVelocity` を設定。地形追従・衝突は移動処理が担当)。到達 or タイムアウトで次へ |
| `escort` | 主役NPC(タップした相手 `eventActor`)が (x,z) まで**先導して歩き**、主人公はその後ろ(`EVENT_FOLLOW_DISTANCE`)を追う。NPC到達かつ主人公が追従距離内で次へ |
| `actorHome` | 主役NPCが**元の位置(`wanderCenter`)へ歩いて戻り**、向きを `homeYaw` に戻す。主人公は待機(見回しのみ) |
| `moveProp` | 可動プロップ(`EventProp`)を (toX,toZ) へ duration 秒かけて smoothstep 補間。コライダーが位置追従するので道が開く |
| `wait` | 一定時間待つ |

主役NPC(`eventActor`)はタップした相手を `TapInteractUseCase` が記録する。escort/actorHome 中は
`TickUseCase` が当該NPCの徘徊をスキップし、`EventService` が直接 `moveTo` で動かす。終了時に `eventActor` を解放。
**イベント中は主役NPCのコライダーを主人公の衝突判定から除外する**(actorHome で戻る NPC が待機中の主人公を
押しのけないようにするため)。

## 進行と操作制限
- `EventService.tick(session, dt)` が `GameSession.activeEvent`(`ActiveEvent`)の現ステップを進める。
- メインループは `activeEvent` 中、`applyStick`/タップ/ダッシュ/滑空を呼ばず `EventService` が駆動する。
  入力アダプタのコールバックも `onLook`(見回し)以外はイベント中ガードする。
- `TickUseCase` はイベント中ポータル通過判定をスキップ(自動歩行の誤通過防止)。
- 移動は既存 `MovementService` を再利用(walkTo は目標速度を置くだけ)。`walkTo` は到達できない場合の
  ソフトロック防止に 12 秒のタイムアウトを持つ。

## 可動プロップ
- `World.props`(`EventProp`)。`TickUseCase` の衝突解決にプロップのコライダー(現在位置から導出)を含める。
- 描画は `ThreeRendererAdapter` が `propMeshes` を毎フレーム位置反映(`syncProps`)。

## 一度きりイベント(once)
`GameEvent.once = true` のイベントは完了すると `GameSession.completedEvents` に記録され、以後は再開しない。
完了後に同じ対象をタップすると、その対象の通常会話(`dialogue`)に切り替わる。
`TapInteractUseCase` は「once 完了済みならイベントを開始せず会話へ」を判定する。

## デモ(昼の世界)
- 「案内役」(6,3): タップ → 主人公を崖の前 (12,12) まで歩いて案内(`day-guide`、繰り返し可)。
- 「岩どかし」(-2,9): タップ → 岩 `day-rock` が (1,7) へ動いて道が開く(`day-rock`、once)。
  どかした後は通常会話(「岩はもうどかしたよ。」)になる。

## 設計(SOLID / クリーンアーキテクチャ)
- domain/values `EventScript`(EventStep/GameEvent/ActiveEvent)・entities `EventProp`・services `EventService`
  は three.js/DOM 非依存。`EventService` は単一責務(イベント進行)で `MovementService` にのみ依存。
- 非イベント時の挙動(会話/扉/ポータル/移動)は不変。

## 関連コード
- `src/domain/values/EventScript.ts` / `src/domain/entities/EventProp.ts` / `src/domain/services/EventService.ts`
- `src/domain/entities/{GameSession,Interactable,Npc,World}.ts`(event/props/activeEvent)
- `src/application/usecases/{TapInteractUseCase,TickUseCase}.ts`(開始・衝突・ポータルスキップ)
- `src/config/worldContent.ts`(EVENTS・PropSpec・NpcSpec.eventId・昼のデモ)
- `src/main.ts`(配線・操作制限・イベントUI)・`src/adapters/rendering/ThreeRendererAdapter.ts`(プロップ描画)

## テスト観点
- EventService: say/wait の時間進行、walkTo の desiredVelocity 設定と到達完了、moveProp の補間と完了、複数ステップ消化と終了。
- EventProp: コライダーが現在位置(y=0)を反映。
