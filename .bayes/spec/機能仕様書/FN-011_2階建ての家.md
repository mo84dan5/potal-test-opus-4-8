# FN-011 2階建ての家(階段で登れる2階)

## 概要
昼の世界の小屋(扉)から入る室内ワールド「2階建ての家」(`two-floor-house`)。
室内の**右側の階段**を昇ると**ロフト(2階)**へ上がれる。

## 仕組み(高さ場による昇降)
本エンジンはプレイヤーの足元 y を毎フレーム `terrain.heightAt(x,z)` へスナップする(FN-008)。
これを利用し、室内の床高さを返す `TwoFloorField`(HeightField)を割り当てることで昇降を実現する。

- **1階**: `h = 0`(玄関・中央前方など)
- **階段**: 右レーン `x ∈ [stairXMin, width/2]`、`z ∈ [stairZBottom, stairZTop]` で `h` を `0 → floorHeight` に線形傾斜
- **ロフト(2階)**: `z >= loftFrontZ` で `h = floorHeight`(部屋の奥側半分)

寸法(`TWO_FLOOR`): 部屋 14×14×6.2m、`floorHeight=3.0`、`loftFrontZ=0`、`stairXMin=3.5`、
階段 `z=-4→0`。

## 転落防止(手すりコライダー)
コライダーは XZ 円柱のみ(y 無視, FN-006)。高さ場が段差になっている縁をそのまま歩くと y が急降下するため、
段差の縁に**手すりコライダー**を置いて越えられないようにする(階段の登り口だけ開ける)。

- ロフト前縁(`z=loftFrontZ`、階段開口 `x>=stairXMin` を除く)に沿った手すり
- 階段の開放側(`x=stairXMin`)に沿った支柱

`twoFloorRailingColliderSpots()` が手すり位置を返し、`main.ts` がコライダー化する。
部屋の外周は `roomWallColliderSpots`(玄関の扉開口つき)で囲う。

## 入退室
- 入室: 昼の世界の小屋(`portalHouses` の1棟, (-16,-2))の扉 `day-twofloor` をタップ(FN-010 と同じ扉=タップ入室)。
  入室時は玄関扉の正面=1階(h=0)に立つ。
- 退室: 1階に降りて玄関の扉 `twofloor-day` をタップ → 昼の世界へ戻る。

## 描画(`buildTwoFloorInterior`)
1階の床・天井・四方の壁(高さ6.2m)、右側の階段(12段)、奥のロフト床スラブ(上面=floorHeight)、
手すり(横バー+支柱)、家具(1階のテーブル/2階のベッド)、暖色の点光源2灯(各階)。

## NPC
2階(ロフト)に住人NPCを配置(`x=-3, z=4` → 高さ場で `h=floorHeight` に乗る)。階段を昇ると会える。

## 関連コード
- `src/domain/values/Terrain.ts` — `TwoFloorField` / `TwoFloorConfig`
- `src/config/worldContent.ts` — `TWO_FLOOR`、`two-floor-house` ワールド、`twoFloorRailingColliderSpots`、`floorKind`、`portalHouses`
- `src/main.ts` — `buildTerrain`(floorKind 分岐)、`twoFloorRailingColliders`
- `src/adapters/rendering/ThreeRendererAdapter.ts` — `buildTwoFloorInterior`

## テスト観点
- `TwoFloorField`: 1階=0 / ロフト=floorHeight / 階段が単調増加・上下端で連続。
- `twoFloorRailingColliderSpots`: ロフト前縁は階段開口を除いて並ぶ / 階段開放側に支柱。
- `two-floor-house` が interior・floorKind='two-floor'・room を持つ / 昼に扉と小屋2棟。
