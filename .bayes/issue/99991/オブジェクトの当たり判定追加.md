# issue-worker: `オブジェクトの当たり判定追加 #99991`

## issue-worker

## オブジェクトの当たり判定追加

### 関連コードPath
| id | file_path | 概要 | 貢献度(初期値=0) |
| --- | --- | --- | --- |
| 0 | src/application/usecases/TickUseCase.ts | L20-31 フレーム更新(移動→ポータル判定)。衝突解決の組込み先 | 1 |
| 1 | src/config/worldContent.ts | オブジェクト配置の共有定義。collisionRadius の追加先 | 1 |
| 2 | src/domain/entities/World.ts | colliders(衝突体一覧)の追加先 | 0 |
| 3 | src/domain/services/MovementService.ts | 移動積分(変更不要の見込み、衝突は別サービスで解決) | 1 |
| 4 | src/main.ts | コライダー構築(オブジェクト+ポータル柱)とDI | 0 |
| 5 | .bayes/spec/機能一覧.md | FN-006 の追加先 | 0 |

### ソースコードを参照した事実確認チェックルール
> **⚠️ 必須実行ルール**
> 各セクションで「参考ソース」または「ソースコード参照」が指定されている場合、**必ず本チェックルールを実行してからドキュメント作成・編集を行うこと**。
> reviewファイルを作成せずにドキュメントを直接編集することは禁止する。
(手順は issue #99998 と同一。reviewファイルは `.bayes/issue/99991/review[連番]_[確認ファイル名].md`)

## Issue Workerの計画

### [o] セクション0 このIssueで何をやりたいかをこのセクションに詳細を書く。

#### 元の依頼文
> オブジェクトに当たり判定をつけて。デプロイまで一気通貫で実施してください

#### やりたいことの詳細
1. **円柱コライダーによる衝突判定**: 木・石・クリスタルに半径つきの円柱コライダー(XZ平面の円)を設定し、プレイヤー(半径0.35m)が通り抜けられないようにする。
2. **滑らか押し出し(スライド)**: めり込んだら法線方向に押し出し、障害物へ向かう速度成分のみ打ち消す(壁ずりで滑らかに移動)。
3. **ポータルは面を通過可能なまま**: ポータル本体にはコライダーを置かず、**枠の左右の柱のみ**にコライダーを設定する(FN-003のシームレス通過と両立)。
4. **クリーンアーキテクチャ維持**: コライダーは domain の値(position+radius)、解決は CollisionService(domainサービス)。配置は worldContent に集約(描画・インタラクションと同一座標)。
5. テスト・ビルド・GitHub Pages 公開・配信確認まで一気通貫。

#### 完了条件(Definition of Done)
- [ ] 木・石・クリスタルを通り抜けられない(押し出し+壁ずり)
- [ ] ポータル面は従来どおり通過できる(枠の柱だけ衝突)
- [ ] 衝突解決がテストで担保されている
- [ ] 仕様書(機能一覧 / FN-006新規)更新
- [ ] テスト全件成功・ビルド成功・GitHub Pages で公開確認

### [o] セクション1 `### 関連コードPath` を作成する
#### 結果
- フレーム更新の組込み箇所(TickUseCase L20-31)とコンテンツ定義・ドメインの拡張箇所を id 0〜5 として登録。

### [o] セクション2 作成予定ファイル、削除予定ファイル一覧を作成する

#### 編集予定ファイル一覧
| file_path | 編集内容 |
| --- | --- |
| src/config/worldContent.ts | 各オブジェクトに collisionRadius を追加 |
| src/domain/entities/World.ts | colliders: readonly Collider[] を追加(デフォルト[]) |
| src/application/usecases/TickUseCase.ts | 移動後・ポータル判定前に衝突解決を実行 |
| src/main.ts | コライダー構築(オブジェクト+ポータル柱×2)とDI |
| src/application/usecases/TickUseCase.test.ts | コンストラクタ変更追従+衝突テスト追加 |
| .bayes/spec/機能一覧.md | FN-006 追加 |
| .bayes/spec/機能仕様書/index.md | FN-006 登録 |

#### 作成予定ファイル一覧
| file_path | 内容 |
| --- | --- |
| src/domain/values/Collider.ts | 円柱コライダーの値定義(position + radius) |
| src/domain/services/CollisionService.ts | 押し出し+速度の壁ずり解決 |
| src/domain/services/CollisionService.test.ts | 衝突解決のテスト |
| .bayes/spec/機能仕様書/FN-006_当たり判定.md | 機能仕様書 |

#### 削除予定ファイル一覧
なし

### [o] セクション3 残りのセクションを作成する。

### [o] セクション4 事実確認と実装・仕様書更新

#### 方針
- review1 で組込み箇所を確認後、domain(Collider/CollisionService)→ TickUseCase → config/main → 仕様書の順で実施。

#### 結果
- review1_衝突解決の組込み箇所確認.md を作成(id 0,1,3 貢献度+1)。組込み位置は「移動積分後・ポータル判定前」と確定。
- domain: Collider(position+radius)、CollisionService(押し出し最大3回反復+壁ずり、プレイヤー半径0.35m)を新設。World に colliders を追加(デフォルト[]で後方互換)。
- TickUseCase: 移動→衝突解決→ポータル判定の順に編成(CollisionService はデフォルト引数で注入)。
- config/main: worldContent の全オブジェクトに collisionRadius(木0.5、石0.55〜0.95、クリスタル0.55)を追加し、main でコライダー構築。ポータルは枠の左右柱(半径0.25)のみ衝突で面は通過可能のまま。
- 仕様書: FN-006_当たり判定.md 新規、機能一覧・機能仕様書indexを更新。

### ソースコードを参照した事実確認
- [x] チェックルールに則ってreviewファイル作成(review1_衝突解決の組込み箇所確認.md)
- [x] ソースコードを参照した事実確認を実行

### [o] セクション5 テスト・ビルド・デプロイ・公開URL確認

#### 方針
- `rtk npm test` → `rtk npm run build` → commit → push → Actions 完了待ち → 公開URLで新バンドル配信確認。

#### 結果
- テスト**67件全て成功**(CollisionService 5件、TickUseCaseの衝突統合1件を追加)。ビルド成功(JS 499.72kB)。
- 初回テストで1件失敗(テスト条件が1ステップでコライダーを通り過ぎる速度設定だった)→ dt を 0.3 に修正して成功。実ゲームは dt クランプにより1フレーム最大0.33m移動でトンネリングなし(FN-006 に制約として明記)。
- commit「オブジェクトに当たり判定を追加」を push、Actions run #27365924502 成功。
- 公開URLで新バンドル `assets/index-DCpJb1K9.js`(200 / 499,718 bytes、ローカルと一致)の配信を確認。

#### 完了条件の検証
- [x] 木・石・クリスタルを通り抜けられない(押し出し+壁ずり、テストで担保)
- [x] ポータル面は従来どおり通過できる(枠の柱だけ衝突。既存のポータル通過テスト9件も全て成功)
- [x] 衝突解決がテストで担保されている(6件追加)
- [x] 仕様書(機能一覧 / FN-006新規)更新
- [x] テスト全件成功・ビルド成功・GitHub Pages で公開確認

## ステータス: ✅ 完了(2026-06-12)
公開URL: https://mo84dan5.github.io/potal-test-fable-05/
